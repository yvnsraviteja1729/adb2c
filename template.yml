parameters:
  - name: whichState
    type: object
  

steps:
  - task: Bash@3
    inputs:
      targetType: 'inline'
      script: |
        #!/bin/bash
        # Write your commands here
        echo 'Hello world'
        ${{ each project in parameters.whichState }}:
        script: "echo 3 ${{ project }}"`
        # echo ${{ parameters.whichState }}
