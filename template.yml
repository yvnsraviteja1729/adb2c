parameters:
- name: listOfStrings
  type: object
  default:{}

steps:
- task: Bash@3
            inputs:
              targetType: 'inline'
              script: |
                # Write your commands here
                echo 'Hello world'
                echo $(listOfStrings)
