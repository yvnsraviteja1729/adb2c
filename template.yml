parameters:
  - name: whichState
    type: object
  

steps:
  - task: Bash@3
    inputs:
      targetType: 'inline'
      script: |
        #!/bin/bash
        # Write your commands here
        echo 'Hello world'
        echo $(whichState)
